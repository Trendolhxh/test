# 首页改版 PRD

> 文档版本：v1.0 | 创建日期：2026-04-15
> 状态：**正式 PRD**
> 所属模块：首页

---

## 一、背景与目标

### 1.1 背景

当前首页以数据仪表盘形式平铺睡眠评分、心率、消耗、高能时段、入睡窗口、睡眠分析等信息，存在以下问题：

1. **缺少主线**：信息平铺无层级，用户需要自行综合判断"今天精力好不好"
2. **核心指标弱化**：高能时长作为产品核心概念，在当前首页中仅作为一个普通卡片存在，未建立起用户心智
3. **缺乏行动指引**：用户看完数据后不知道该做什么，产品停留在"告诉你"而非"引导你"
4. **场景未区分**：晨起和睡前是两个截然不同的使用场景，当前首页用同一套布局应对

### 1.2 目标

将首页从"数据仪表盘"改版为**以高能时长为核心、区分状态查看与行动管理的双 Tab 结构**：

| 目标 | 衡量方式 |
|:---|:---|
| 用户 3 秒内获得今日精力结论 | 首页首屏聚焦核心指标变动，不需要滑动 |
| 强化高能时长概念认知 | 高能时长作为首页恒定锚点，每次打开都看到 |
| 提供可执行的行为建议 | 今日 Tab 以时间轴 + 行为 pin 呈现具体建议 |
| 覆盖晨起和睡前两个核心场景 | 状态 Tab 白天/夜间自动切换核心指标 |

### 1.3 设计原则

| 原则 | 说明 |
|:---|:---|
| **结论先行** | 核心指标的变动和好坏判断在首屏第一眼完成，不依赖用户解读 |
| **行为归一** | 所有行动建议统一为"行为 + 时间点 + 剂量"格式 |
| **场景自适应** | 同一套交互结构，通过数据内容切换适应白天/夜间场景 |
| **可视化优先** | 核心信息通过图形和数据可视化传递，文字摘要作为辅助 |
| **拓展性** | 新增归因因子、身体指标、行为类型时，不需要改变首页结构 |

---

## 二、首页架构：双 Tab 结构

### 2.1 Tab 定义

首页分为两个平级 Tab，用户可左右切换：

| Tab | 名称 | 核心问题 | 定位 |
|:---|:---|:---|:---|
| 状态 | 状态 | "我怎么样？" | 查看精力状态、归因分析、身体数据 |
| 今日 | 今日 | "我该做什么？" | 查看时间轴、行动建议、精力管理 |

### 2.2 默认行为

- **默认 Tab**：状态 Tab（用户 70% 的场景只需要看一眼核心指标）
- **记忆上次 Tab**：不记忆。每次打开 App 始终回到状态 Tab
- **切换方式**：顶部 Tab 栏点击切换，或左右滑动切换

### 2.3 数据同步与 Tab 的关系

数据同步状态层（参见 `data-sync-status-prd.md`）位于 Tab 栏上方，作为全局指示，不随 Tab 切换变化。同步完成后，两个 Tab 的数据同步更新。

```
┌─────────────────────────────────────┐
│  [全局同步状态条]                     │  ← 同步中 / 数据过期时显示
├─────────────────────────────────────┤
│  [ 状态 ]    [ 今日 ]                │  ← Tab 栏
├─────────────────────────────────────┤
│                                     │
│  （当前 Tab 内容区）                  │
│                                     │
└─────────────────────────────────────┘
```

---

## 三、状态 Tab

### 3.1 整体信息结构

状态 Tab 的信息结构自上而下为五层，形成**核心指标 → 解读 → 归因 → 异常 → 完整数据**的递进关系：

```
┌─────────────────────────────────────┐
│  第一层：核心指标变动                  │  ← 首屏核心，3 秒结论
│  第二层：Smart Digest                │  ← AI 一句话解读 + Agent 入口
│  第三层：数据归因                     │  ← 各因子对核心指标的贡献
│  第四层：值得注意的值                  │  ← 异常数据高亮（仅异常时出现）
│  第五层：完整身体数据                  │  ← 当前数据 + 睡眠数据图表
└─────────────────────────────────────┘
```

用户的典型浏览深度：
- **70% 的场景**：只看第一层（核心指标），确认"今天怎么样"后关闭
- **20% 的场景**：看到第二层（Smart Digest），被一句话吸引后点击进入 Agent 对话
- **10% 的场景**：滑到第三~五层，深入了解归因和完整数据

### 3.2 第一层：核心指标变动

#### 3.2.1 白天模式（默认）

核心指标为**今日高能时长**，以小时数直接展示，配合与昨日的变化量。

**展示要素**：

| 要素 | 说明 | 示例 |
|:---|:---|:---|
| 高能时长数值 | 今日预估/实际高能时长，大字展示 | **7.5h** |
| 变化量 | 与昨日的差值，带方向 | **+0.3h** / **-1.2h** |
| 高能时段分布 | 具体时间段 | 09:30–11:45 · 18:45–21:10 |

**好坏判断的可视化规则**：

高能时长不使用抽象评分。通过以下规则让用户直观感知好坏：

| 条件 | 视觉表现 | 说明 |
|:---|:---|:---|
| ≥ 10h | 强正向视觉（如品牌色高亮 + 向上箭头） | 精力充沛 |
| 4h–10h | 常规视觉 | 正常范围 |
| < 4h | 警示视觉（如暖色提示） | 精力不足 |
| 日变化 ≥ +1h | 正向变化强调（如绿色 + 上升标记） | 明显提升 |
| 日变化 ≤ -1h | 负向变化强调（如橙色 + 下降标记） | 明显下降 |
| 日变化 -1h ~ +1h | 平稳视觉，不特别强调 | 正常波动 |

> 设计要求：用户不读数字，仅凭颜色和方向即可判断"今天好不好"。

#### 3.2.2 夜间模式

当最后一个高能时段结束后，核心指标自动从"今日高能时长"切换为**入睡准备度**。

**切换触发条件**：当日所有预测高能时段均已结束（以最后一个高能时段的 `end` 时间为准）。

**展示要素**：

| 要素 | 说明 | 示例 |
|:---|:---|:---|
| 入睡准备度 | 定性状态指示，配合可视化 | 🟢 已准备好 / 🟡 接近准备好 / 🔴 还需准备 |
| 明日高能预估 | 基于当前状态入睡后的预测 | 明日预估高能 **7.5h** |
| 推荐入睡时间 | 复用睡眠窗口数据 | 推荐 22:30 入睡 |

**入睡准备度三档定义**：

| 状态 | 视觉 | 判定条件（算法输出） | 附加信息 |
|:---|:---|:---|:---|
| 已准备好 | 🟢 + 安静的正向视觉 | 综合准备度指数 ≥ 阈值 A | 无额外建议 |
| 接近准备好 | 🟡 + 中性视觉 | 准备度指数介于阈值 B–A 之间 | 显示"预计约 X 分钟后进入最佳状态" |
| 还需准备 | 🔴 + 轻度提示视觉 | 准备度指数 < 阈值 B | 显示一条简短建议（如"试试 5 分钟呼吸练习"） |

> 设计要求：🟢 和 🟡 状态下不主动给行动建议——睡前场景的体验目标是"安心"，不是"被要求做事"。只有 🔴 状态才出现建议。

**模式切换的视觉过渡**：
- 切换时不做突兀的页面替换，而是核心指标区域的内容平滑过渡
- 下方的归因、值得注意的值等层级结构保持不变，仅内容替换为睡眠准备相关

### 3.3 第二层：Smart Digest

复用现有 Smart Digest 能力（参见 `homepage_smart_digest.md`），位置调整为**核心指标下方**。

**展示形式**：
- 一行 AI 生成的文字（≤ 50 字），对核心指标变动的人话解读
- 整行可点击，点击后展开进入 Agent Chatflow 对话

**与现有 Smart Digest PRD 的差异**：

| 维度 | 原方案 | 改版后 |
|:---|:---|:---|
| 位置 | 问候语下方副标题 | 核心指标下方 |
| 触发条件 | 不变（app_open + data_refreshed + 冷却期 + 数据有变化） | 不变 |
| 内容生成 | 不变（基于 24h 数据 diff 生成） | 不变 |
| 交互 | 快捷提问按钮点击进入 Chatflow | Smart Digest 文字本身可点击展开进入 Chatflow；快捷提问按钮移至第四层"值得注意的值"附近 |

**Smart Digest 在夜间模式下的适配**：
- 夜间模式下，Smart Digest 的生成输入数据中包含入睡准备度相关指标，摘要内容自然偏向睡眠准备方向
- 示例："静息心率已经降到 58 了，身体比你先准备好了"

### 3.4 第三层：数据归因

归因层解释核心指标"为什么是这个值"，将影响因子拆解展示。

#### 3.4.1 白天模式归因

归因对象：**今日高能时长**

**归因因子**：

| 因子 | 数据来源 | 贡献方向示例 |
|:---|:---|:---|
| 昨夜睡眠时长 | Apple Health 睡眠数据 | 睡了 7h12m → +0.2h |
| 深睡占比 | Apple Health 睡眠数据 | 深睡 22%（↑3%）→ +0.1h |
| 昨日运动 | Apple Health 活动数据 | 跑步 30min → +0.2h |
| 咖啡因摄入 | 用户记录 / 自动检测 | 10:00 1 杯 → 中性 |
| 入睡时间偏差 | Apple Health 睡眠数据 | 比推荐晚 45min → -0.3h |

**展示规则**：
- 每个因子一行：因子名称 + 具体数据 + 对高能时长的贡献方向和幅度
- 贡献为正用正向视觉标记，贡献为负用负向视觉标记，中性不标记
- 因子按贡献绝对值排序，影响最大的排最前
- 可视化形式建议：紧凑的横向条形图或瀑布图，让用户直观看到各因子的正负贡献

**拓展性**：未来新增归因因子（光照时长、CO2 浓度、药物等），只需在此列表中增加一行，不影响整体结构。

#### 3.4.2 夜间模式归因

归因对象：**入睡准备度**

**归因因子**：

| 因子 | 数据来源 | 贡献方向示例 |
|:---|:---|:---|
| 心率趋势 | Apple Health 实时心率 | 心率持续下降中 → 正向 |
| 距上次咖啡因 | 用户记录 / 自动检测 | 距上次咖啡因 8h → 正向 |
| 距上次运动 | Apple Health 活动数据 | 距上次运动 5h → 正向 |
| 当日活动量 | Apple Health 活动数据 | 步数 8000+ → 正向 |
| 当前时间 vs 推荐入睡 | 睡眠窗口算法 | 距推荐入睡还有 30min → 中性 |

**展示规则**：同白天模式——每个因子一行，带贡献方向，正向因子让用户知道"我准备好了"，负向因子让用户知道"什么在影响我入睡"。

### 3.5 第四层：值得注意的值

当某个身体指标出现异常（特别好或特别坏）时，以卡片形式凸显。

**触发规则**：

| 异常类型 | 判定条件示例 | 展示方式 |
|:---|:---|:---|
| 正向异常 | 深睡占比 28%，超过近 30 天 P90 | 正向卡片（品牌色边框），标题 + 一句说明 |
| 负向异常 | 静息心率 +8bpm，超过近 7 天均值 2 个标准差 | 警示卡片（暖色边框），标题 + 一句说明 |
| 高能时长异常波动 | 日变化 ≥ ±1h | 在第一层核心指标处已有视觉强调，此处不重复 |

**交互**：
- 每个异常卡片可点击 → 进入 Agent Chatflow，自动注入该异常数据作为对话上下文
- 注入方式同 Smart Digest：异常描述作为 assistant 首条消息，用户的点击等同于一条预设提问（如"静息心率为什么偏高"）

**无异常时**：第四层不显示，页面自然收缩，第三层直接连接第五层。

**Smart Digest 快捷提问**：3 个快捷提问按钮放置在本层附近（有异常时在异常卡片下方，无异常时在第三层下方），作为进入 Chatflow 的低摩擦入口。

### 3.6 第五层：完整身体数据

分为两个子区块，提供完整的身体数据查看：

#### 3.6.1 当前身体数据

| 指标 | 可视化形式 | 说明 |
|:---|:---|:---|
| 心率 | 实时数值 + 日内趋势折线图 | 静息心率高亮标注 |
| HRV | 数值 + 近 7 天趋势折线图 | 标注健康参考区间 |
| 活动量 | 今日步数 + 活动消耗 | 环形进度或条形 |
| 血氧 | 平均值 + 最低值 | 异常低值时高亮 |

#### 3.6.2 睡眠数据

| 指标 | 可视化形式 | 说明 |
|:---|:---|:---|
| 睡眠时长 | 数值 + 近 7 天趋势柱形图 | 标注推荐时长区间 |
| 睡眠阶段 | 水平堆叠条形图（深睡/浅睡/REM/清醒） | 各阶段占比和时长 |
| 入睡/起床时间 | 数值 + 近 7 天一致性散点图 | 偏差过大时标注 |
| 睡眠效率 | 百分比 + 近 7 天趋势 | 标注健康参考区间 |

> 此区块为深度查看需求服务，大多数用户不会每天翻到这里。数据均来自 Apple Health 同步结果。

### 3.7 夜间模式切换

#### 3.7.1 切换时机

- **触发条件**：当日所有预测高能时段的最后一个 `end` 时间已过
- **示例**：高能时段为 09:30–11:45 和 18:45–21:10，则 21:10 后自动切换为夜间模式
- **反向切换**：次日 05:00 后首次打开 App 自动恢复为白天模式（参照日期分界线规则，见 `sleep-window-prd.md` 2.1.1）

#### 3.7.2 切换内容对照

| 层级 | 白天模式 | 夜间模式 |
|:---|:---|:---|
| 第一层 | 今日高能时长 + 变化量 | 入睡准备度 + 明日高能预估 + 推荐入睡时间 |
| 第二层 | Smart Digest（高能 / 睡眠相关） | Smart Digest（入睡准备相关） |
| 第三层 | 高能时长归因因子 | 入睡准备度归因因子 |
| 第四层 | 身体指标异常 | 入睡相关指标异常 |
| 第五层 | 不变 | 不变 |

> 交互结构完全一致，用户学一次就会用，夜间模式不引入新的交互模式。

### 3.8 重要日场景（2b）

**场景描述**：用户明天有重要事，想确保明日精力最好。

**入口**：状态 Tab 进入夜间模式后，Smart Digest 或快捷提问中出现相关引导（如"明天有重要的事怎么准备"）。

**用户流程**：

```
状态 Tab（夜间模式）
    ↓ 用户点击 Smart Digest 或快捷提问
    ↓
进入 Agent Chatflow
    ↓ Agent 基于当前入睡准备度和身体数据
    ↓ 生成个性化的行动建议方案
    ↓ 建议格式：行为 + 时间点 + 剂量
    ↓ 例："22:00 做 10 分钟呼吸练习 → 入睡准备度预计提升 → 明日高能预估 +0.3h"
    ↓
Agent 展示"一键添加到今日"按钮
    ↓ 用户点击
    ↓
行动建议作为 pin 添加到今日 Tab 的时间轴
```

**体验目标**：专业指导 + 掌控感。Agent 提供因果透明的建议（做什么 → 改善多少），用户选择后能一键添加到行动计划中。

---

## 四、今日 Tab

### 4.1 整体信息结构

今日 Tab 是用户的**精力管理工具**，以**纵向 24 小时时间轴**为核心，时间从上往下流动，将睡眠时段、能量区间、行动建议统一展示在一条可滚动的垂直时间线上。

```
┌──────────────────────────────────────────────┐
│  hh ┌────────────────────────┐               │
│     │  Sleep 区块（紫色）     │ ┃ 睡眠阶段条  │  ← 昨夜睡眠数据
│  hh │                        │ ┃             │
│     └────────────────────────┘               │
│  hh ┌────────────────────────┐               │
│     │  Grogginess（惯性区）   │               │  ← 起床后低能过渡
│     └────────────────────────┘               │
│  hh ┌────────────────────────┐  ☕ 喝咖啡    │
│     │  Morning Peak（品牌色） │               │  ← 高能区间
│  hh │  ┃ 能量曲线（纵向）     │               │
│     └────────────────────────┘               │
│  hh    （低能间歇）            🏃 运动        │
│  hh ┌────────────────────────┐               │
│     │  Afternoon Peak        │               │  ← 高能区间
│     └────────────────────────┘               │
│  hh ┌────────────────────────┐  🌙 入睡准备   │
│     │  Wind-down（放松区）    │               │  ← 入睡准备期
│  hh ├────────────────────────┤               │
│     │  Melatonin Window      │               │  ← 入睡窗口（高亮）
│  hh └────────────────────────┘               │
│     Sleep need: 8h 30m                       │  ← 末端睡眠需求
└──────────────────────────────────────────────┘
     ↑ 时间标签（每小时）         ↑ 行动 pin（浮动标签）
```

### 4.2 纵向 24h 能量时间轴

#### 4.2.1 核心定义

一条从上到下覆盖 24 小时的**纵向可滚动时间轴**，是今日 Tab 的视觉主体。用户通过上下滑动浏览全天。

**布局结构**：

| 要素 | 说明 |
|:---|:---|
| 时间轴方向 | **纵向**，时间从上到下流动 |
| 起点 | 用户常规起床时间前 1h（如常规 06:30 起床，从 05:30 开始） |
| 终点 | 次日同一时间（覆盖完整 24h + 入睡时段） |
| 左侧时间标签 | 每小时一个标签（05、06、07…00、01），固定在时间轴左侧边缘 |
| 中央区域 | **时段区块**：按能量状态划分为不同区块，依次排列 |
| 右侧 | **行动 pin**：以浮动标签形式钉在对应时间位置 |
| 能量曲线 | 在高能区块内以纵向曲线展示能量走势（X=能量水平，Y=时间） |

#### 4.2.2 时段区块类型

时间轴由以下类型的区块按时间顺序排列组成：

| 区块类型 | 视觉表现 | 内容 | 触发条件 |
|:---|:---|:---|:---|
| **Sleep（睡眠）** | 深紫色背景 | 时段名 + 时长；右侧嵌入睡眠阶段可视化条（深睡/浅睡/REM/清醒，纵向堆叠色条） | 昨夜睡眠时段 |
| **Grogginess（惯性区）** | 暗灰色，波浪纹理背景 | 时段名 + 预计持续时长 | 起床后的低能过渡期（通常 30min–2h） |
| **High Energy Peak（高能区间）** | **品牌色（绿色）高亮背景** | 时段名（如 "Morning Peak"）+ 起止时间；内嵌纵向能量曲线 | 能量预测/实际高于高能阈值的区间 |
| **Low Energy（低能间歇）** | 无特殊背景，时间轴默认底色 | 仅显示时间刻度线 | 两个高能区间之间的间歇 |
| **Wind-down（放松区）** | 暗色调背景 | 时段名 + "入睡准备" | 推荐入睡时间前 1–2h |
| **Melatonin Window（入睡窗口）** | **高亮紫色背景**，配 🌙 图标 | 时段名 + 推荐入睡时间 + 明日高能预估 | 推荐入睡时间点，可点击进入设置页 |
| **Sleep Need（末端）** | 时间轴底部元素 | "Sleep need: Xh Ym" | 始终显示在末端 |

**区块之间无间隙**，形成连续的 24h 时间线。

#### 4.2.3 时间轴的三种状态

**预测态**（晨起，当日首次打开）：

| 内容 | 说明 |
|:---|:---|
| 所有区块 | 均为预测数据，以统一样式展示 |
| 能量曲线 | 全天曲线统一为虚线/浅色 |
| 行动 pin | 所有当日建议 pin 可见，状态为"待执行" |
| 自动定位 | 滚动到起床时间附近 |

**实时进度态**（日间打开）：

| 内容 | 说明 |
|:---|:---|
| 当前时间标记 | 一条横线 + 红色圆点 + "现在" 标签，贯穿时间轴宽度 |
| 上方（已过） | 区块和曲线以实线展示（实际数据） |
| 下方（未来） | 区块和曲线以虚线/浅色展示（预测数据） |
| 行动 pin | 已过的标记完成/跳过，未来的正常展示 |
| 自动定位 | 滚动到当前时间标记附近 |

**回顾态**（睡前/夜间打开）：

| 内容 | 说明 |
|:---|:---|
| 所有区块 | 均为实际数据，实线展示 |
| 能量曲线 | 全天实线 |
| 行动 pin | 全部标记为已完成/已跳过 |
| 末端 | Melatonin Window 和 Sleep Need 突出展示 |
| 自动定位 | 滚动到晚间入睡窗口附近 |

#### 4.2.4 高能区块内的能量曲线

高能区块内嵌一条纵向能量曲线，方向旋转 90°（Y 轴 = 时间，X 轴 = 能量水平）：

- 使用贝塞尔曲线或样条插值，不使用折线
- 曲线向右延伸表示能量越高，向左表示能量越低
- 高能区块的填色区域 = 曲线右侧到区块右边界的面积（品牌色半透明填充）
- 低能间歇区域不展示曲线，仅保留时间刻度

#### 4.2.5 可视化要求

| 要求 | 说明 |
|:---|:---|
| 曲线平滑 | 使用贝塞尔曲线或样条插值，不使用折线 |
| 滚动交互 | 支持**垂直滑动**浏览全天；自动定位到当前时间/关键区域 |
| 每小时高度 | 约 80px，整个 24h 时间轴总高度约 1920px，需要滚动查看 |
| 性能 | 区块和曲线渲染 ≤ 1 帧（16ms），滚动流畅 60fps |
| 响应式 | 适配屏幕宽度，时间轴占满 Tab 内容区宽度 |

### 4.3 行动建议事件

#### 4.3.1 事件格式

所有行动建议统一归一化为三要素：

| 要素 | 说明 | 示例 |
|:---|:---|:---|
| **行为** | 建议的具体行为，配合图标 | ☕ 喝咖啡、🏃 运动、😴 小睡、🧘 冥想、🌙 准备入睡 |
| **时间点** | 建议执行的时间或时间窗口 | 10:30 前、17:00–18:00、22:30 |
| **剂量** | 建议的量或时长 | 1 杯、30min、20min |

#### 4.3.2 Pin 在时间轴上的展示

- 每个行动建议以**浮动标签**的形式钉在时间轴**右侧**对应时间位置
- Pin 的视觉形式：圆角小标签，内含行为类型图标（☕🏃😴🧘🌙）+ 简短标签（如"喝咖啡"）
- Pin 的纵向位置对应其建议时间，自然嵌入时间线视觉流中——咖啡 pin 在高能区间前，用户直观理解"为什么是这个时间"
- 多个 pin 时间接近时，自动向下排布避免重叠

#### 4.3.3 Pin 的状态

| 状态 | 视觉表现 | 说明 |
|:---|:---|:---|
| 待执行 | 正常颜色 + 实心图标 | 时间点未到 |
| 当前建议 | 高亮 + 脉动效果（可选） | 距最近的下一个建议 |
| 已完成 | 浅色 + ✓ 标记 | 用户已执行（自动检测或手动标记） |
| 已跳过 | 浅色 + 删除线 | 时间已过但未执行 |

#### 4.3.4 Pin 的交互

点击 pin 展开详情浮层（在时间轴上方或下方弹出）：

| 详情内容 | 说明 |
|:---|:---|
| 行为 + 时间 + 剂量 | 完整信息 |
| 原因说明 | 一句话解释（如"10:30 前摄入咖啡因不影响晚间睡眠"） |
| 替代方案 | 可选，如"改为绿茶（咖啡因更低）" |
| 对高能的预估影响 | 可选，如"预计贡献 +0.3h 高能" |
| 进入 Chatflow | "了解更多"按钮 → 进入 Agent 对话 |

#### 4.3.5 建议来源与完成判定

**建议来源**：

| 来源 | 说明 |
|:---|:---|
| 算法自动生成 | 基于用户睡眠数据、生理节律、历史行为模式自动生成当日建议 |
| Agent 对话生成 | 2b（重要日）场景中 Agent 生成的建议，用户一键添加 |
| 用户手动添加 | 未来支持用户自己添加事件 |

**完成判定**：

| 行为类型 | 判定方式 |
|:---|:---|
| 运动 | Apple Health 检测到运动记录 → 自动标记完成 |
| 小睡 | Apple Health 检测到睡眠记录（白天） → 自动标记完成 |
| 咖啡因 | V1 不自动检测，时间过后标记为"已跳过"；用户可手动标记完成 |
| 冥想 | V1 不自动检测，时间过后标记为"已跳过"；用户可手动标记完成 |
| 准备入睡 | 不标记状态，作为时间提示存在 |

> 未来自有硬件和更多数据源接入后，可提升自动检测覆盖率。

### 4.4 入睡窗口集成

#### 4.4.1 在时间轴上的展示

入睡窗口作为时间轴中的 **Melatonin Window 区块**展示（参见 4.2.2 区块类型定义）：

- 位置：时间轴上推荐入睡时间对应位置，紧接 Wind-down 区块之后
- 视觉：高亮紫色背景 + 🌙 图标，区块内显示推荐入睡时间和明日高能预估
- 右侧同时显示一个浮动 pin 标签（"Melatonin window"），视觉权重高于普通行动 pin
- 随一天推进，用户滚动到晚间时入睡窗口区块自然进入视野

#### 4.4.2 点击进入设置页

点击入睡窗口标记 → 进入睡眠窗口设置页（复用现有 `sleep-window-prd.md` 定义的独立页面）。

设置页功能不变：
- 圆盘时钟调整入睡/起床时间
- 实时预览明日高能时长变化
- 设置入睡提醒和起床闹钟

#### 4.4.3 与状态 Tab 的联动

用户在入睡窗口设置页调整时间后：
- 状态 Tab 夜间模式的"推荐入睡时间"和"明日高能预估"同步更新
- 今日 Tab 时间轴上的入睡窗口标记位置和预估数值同步更新

### 4.5 熬夜场景（2c）

**场景描述**：用户今晚需要晚睡，想知道不同入睡时间对明日精力的影响，选择一个损失最小的方案。

**用户流程**：

```
今日 Tab → 点击时间轴上的入睡窗口标记
    ↓
进入睡眠窗口设置页
    ↓ 用户拖动圆盘将入睡时间从 22:30 调到 01:00
    ↓ 实时预览：明日高能从 7.5h 降到 5.8h（-1.7h）
    ↓ 继续尝试 02:00 → 明日高能 4.5h（-3.0h）
    ↓ 回到 01:00（性价比最高）
    ↓
确认今晚入睡窗口为 01:00
    ↓
返回今日 Tab
    ↓ 时间轴上入睡窗口位置更新到 01:00
    ↓ 如果入睡提醒已开启，提醒时间同步更新
```

**体验目标**：务实信任。不说教"你不应该熬夜"，直接展示不同选择的代价（明日高能变化），让用户自己做最优判断。

**设置页改版要求**：

现有设置页（`sleep-window-prd.md`）已支持拖动调整和实时预览，可完整复用。唯一需要确认的是：
- 当用户选择的入睡时间远晚于推荐时间（如晚 2h 以上），文案上保持"中性呈现差值"原则，不增加负面措辞或警告

### 4.6 未来拓展

今日 Tab 的时间轴 + 行动 pin 架构具备良好的拓展性：

| 拓展方向 | 时间线 | 说明 |
|:---|:---|:---|
| **日历同步** | V2 | 用户授权日历后，日程事件显示在时间轴上。系统根据日程自动调整行为建议（如"下午有重要会议，建议 14:00 喝咖啡"） |
| **事件自动检测** | V2 | 运动结束、小睡结束等事件被自动检测后，在时间轴上生成"已完成"的事件 pin，配合高能时长的实时更新形成正反馈 |
| **手动记录** | V2 | 用户可在时间轴上手动添加事件（喝了咖啡、吃了药、喝了酒），系统将其纳入后续建议的调整因素 |
| **药物影响** | V3 | 药物摄入事件可关联药物数据库，预估对能量曲线的影响，在时间轴上可视化展示 |
| **环境数据** | V3 | 自有硬件提供光照、CO2 数据后，环境因素作为新的 pin 类型出现（如"现在开窗通风 15min → CO2 ↓"） |

---

## 五、跨 Tab 关联

### 5.1 数据一致性

两个 Tab 共享同一套底层数据，以下数据点必须保持一致：

| 数据点 | 状态 Tab | 今日 Tab |
|:---|:---|:---|
| 高能时长 | 第一层核心指标 | 时间轴高能区间的总时长 |
| 高能时段分布 | 第一层时段展示 | 时间轴上的高亮区间 |
| 推荐入睡时间 | 夜间模式第一层 | 时间轴末端入睡窗口标记 |
| 明日高能预估 | 夜间模式第一层 | 入睡窗口标记上的预估值 |

当用户在入睡窗口设置页调整时间后，两个 Tab 的相关数据同步更新。

### 5.2 跨 Tab 操作

| 操作 | 起点 | 终点 |
|:---|:---|:---|
| 2b 一键添加行动建议 | 状态 Tab → Chatflow → Agent 生成建议 | 建议作为 pin 添加到今日 Tab 时间轴 |
| 入睡窗口调整 | 今日 Tab → 入睡窗口设置页 → 确认 | 状态 Tab 夜间模式的推荐时间和预估同步更新 |

### 5.3 Smart Digest 的 Tab 归属

- Smart Digest 仅出现在 **状态 Tab**（第二层）
- 今日 Tab 是纯工具型页面，不展示 AI 文字摘要
- Smart Digest 的快捷提问按钮仅在状态 Tab 展示

---

## 六、场景体验目标

以下体验目标作为设计评审的验收标准。每个场景的设计方案必须满足"核心体验"和"调性"要求，不得出现"反面"列所描述的感受。

| 场景 | 触发条件 | 核心体验 | 调性 | 反面（不应有的感受） |
|:---|:---|:---|:---|:---|
| **晨起** | 当日首次打开 + 05:00–12:00 | 掌控感 + 效率 | 清晰、果断、轻快 | 信息过载、需要花时间理解 |
| **日间查看** | 非首次打开 / 12:00 后 | 效率 + 即时行动力 | 即时、具体、一步到位 | 信息过多、找不到重点 |
| **睡前常规** | 夜间模式激活后 | 安心 | 安静、温暖、确认 | 焦虑、被要求做事 |
| **重要日前夜** | 用户主动触发（Chatflow） | 专业指导 + 掌控 | 专业、可控、即时反馈 | 无力感、选择困难 |
| **需要熬夜** | 用户调整入睡窗口 | 务实信任 | 不评判、策略感 | 被说教、内疚感 |
| **活动结束** | 检测到运动/小睡/冥想结束（V2） | 成就感 + 正反馈 | 仪式感、数字变化 | 无感、被淹没 |

---

## 七、与现有功能的关系

### 7.1 与 Smart Digest 的关系

| 维度 | 原方案（改版前） | 改版后 |
|:---|:---|:---|
| 位置 | 首页问候语下方副标题 | 状态 Tab 第二层（核心指标下方） |
| 触发逻辑 | 不变 | 不变（参见 `homepage_smart_digest.md` 2.1–2.4） |
| 生成内容 | 不变 | 不变（digest + questions） |
| 快捷提问位置 | 输入框上方 | 状态 Tab 第四层附近 |
| Chatflow 注入 | 不变 | 不变（摘要作为 assistant 首条消息注入） |
| 今日 Tab | — | 不展示 Smart Digest |

### 7.2 与睡眠窗口的关系

| 维度 | 原方案（改版前） | 改版后 |
|:---|:---|:---|
| 入口 | 首页「入睡窗口」卡片 | 今日 Tab 时间轴上的入睡窗口标记 |
| 功能页 | 独立原生页面（不变） | 不变（参见 `sleep-window-prd.md`） |
| 数据展示 | 首页卡片展示推荐入睡时间 | 状态 Tab 夜间模式展示 + 今日 Tab 时间轴标记 |
| 设置结果联动 | 仅影响推送提醒 | 同时更新两个 Tab 的相关数据 |

### 7.3 与数据同步状态的关系

| 维度 | 原方案（改版前） | 改版后 |
|:---|:---|:---|
| 位置 | 首页顶部全局指示条 | Tab 栏上方全局指示条（不随 Tab 切换） |
| 同步范围 | 不变 | 不变（所有健康数据并行同步） |
| 同步完成后 | 触发 Smart Digest + 更新数据卡片 | 触发 Smart Digest + 更新两个 Tab 的所有数据 |
| 同步失败 | 数据卡片展示缓存值 | 两个 Tab 均展示缓存值 |

### 7.4 与 Onboarding 的关系

- Onboarding 完成后首次进入首页时，触发高能时长概念教育（首次引导浮层）
- 引导内容：解释高能时长是什么、怎么看变化、今日 Tab 的建议怎么用
- 引导完成后进入正常首页状态
- Onboarding 期间 Tab 栏不展示（参见 `onboarding-ui-prd.md` 导航锁定规则）

---

## 八、数据依赖与接口需求

### 8.1 状态 Tab 所需数据

#### 现有接口（可复用）

| 数据 | 来源 | 说明 |
|:---|:---|:---|
| 高能时长 + 高能时段分布 | 后端计算（依赖睡眠 + 心率数据） | 参见 `data-sync-status-prd.md` 同步流程 |
| Smart Digest | LLM 单轮调用 | 参见 `homepage_smart_digest.md` |
| 推荐入睡时间 + 明日高能预估 | `/api/sleep-window/init` | 参见 `sleep-window-prd.md` |
| 睡眠数据（时长、阶段、效率等） | Apple Health 同步 | 现有数据管道 |
| 心率 / HRV / 活动 / 血氧 | Apple Health 同步 | 现有数据管道 |

#### 新增接口需求

**1. 高能时长归因接口**

```
GET /api/high-energy/attribution

Response:
{
  "high_energy_h": 7.5,
  "high_energy_h_yesterday": 7.2,
  "delta_vs_yesterday": 0.3,
  "factors": [
    {
      "name": "sleep_duration",
      "label": "昨夜睡眠时长",
      "value": "7h12m",
      "contribution_h": 0.2,
      "direction": "positive"
    },
    {
      "name": "deep_sleep_pct",
      "label": "深睡占比",
      "value": "22%",
      "value_change": "+3%",
      "contribution_h": 0.1,
      "direction": "positive"
    },
    {
      "name": "bedtime_deviation",
      "label": "入睡时间偏差",
      "value": "比推荐晚 45min",
      "contribution_h": -0.3,
      "direction": "negative"
    }
    // ... 更多因子
  ],
  "notable_values": [
    {
      "metric": "deep_sleep_pct",
      "label": "深睡占比",
      "value": "28%",
      "context": "近 30 天最佳",
      "type": "positive",
      "agent_prompt": "我的深睡占比为什么今天特别高"
    }
    // 可能为空数组
  ]
}
```

**2. 入睡准备度接口**

```
GET /api/sleep-readiness

Response:
{
  "readiness_level": "ready",       // "ready" | "almost" | "not_ready"
  "estimated_ready_in_min": null,   // 仅 "almost" 时返回，如 20
  "suggestion": null,               // 仅 "not_ready" 时返回，如 "试试 5 分钟呼吸练习"
  "predicted_tomorrow_high_energy_h": 7.5,
  "factors": [
    {
      "name": "heart_rate_trend",
      "label": "心率趋势",
      "value": "持续下降中",
      "direction": "positive"
    },
    {
      "name": "caffeine_elapsed",
      "label": "距上次咖啡因",
      "value": "8h",
      "direction": "positive"
    },
    {
      "name": "exercise_elapsed",
      "label": "距上次运动",
      "value": "5h",
      "direction": "positive"
    },
    {
      "name": "activity_level",
      "label": "当日活动量",
      "value": "步数 8234",
      "direction": "positive"
    }
  ]
}
```

**3. 今日行动建议接口**

```
GET /api/today/suggestions

Response:
{
  "suggestions": [
    {
      "id": "sug_001",
      "type": "caffeine",
      "icon": "☕",
      "label": "喝咖啡",
      "time": "10:30",
      "time_type": "before",         // "before" | "at" | "window"
      "dosage": "1 杯",
      "reason": "10:30 前摄入咖啡因不影响晚间睡眠",
      "impact_h": 0.3,               // 对高能时长的预估影响
      "status": "pending",           // "pending" | "current" | "done" | "skipped"
      "source": "algorithm"          // "algorithm" | "agent" | "user"
    },
    {
      "id": "sug_002",
      "type": "exercise",
      "icon": "🏃",
      "label": "运动",
      "time": "17:00",
      "time_type": "at",
      "time_end": "18:00",
      "dosage": "30min",
      "reason": "下午运动可提升傍晚高能时段",
      "impact_h": 0.5,
      "status": "pending",
      "source": "algorithm"
    },
    {
      "id": "sug_003",
      "type": "sleep_prep",
      "icon": "🌙",
      "label": "准备入睡",
      "time": "22:30",
      "time_type": "at",
      "dosage": null,
      "reason": null,
      "impact_h": null,
      "status": "pending",
      "source": "algorithm"
    }
  ]
}
```

**4. 建议状态更新接口**

```
POST /api/today/suggestions/{id}/status

Request:
{
  "status": "done"    // "done" | "skipped"
}
```

**5. 能量曲线数据接口**

```
GET /api/today/energy-curve

Response:
{
  "curve_points": [
    { "time": "06:00", "energy": 0.3, "type": "actual" },
    { "time": "06:30", "energy": 0.45, "type": "actual" },
    // ...
    { "time": "14:00", "energy": 0.7, "type": "predicted" },
    // ... 每 30 分钟一个点
  ],
  "high_energy_windows": [
    { "start": "09:30", "end": "11:45" },
    { "start": "18:45", "end": "21:10" }
  ],
  "sleep_window": {
    "start": "23:15",    // 昨夜入睡
    "end": "06:28"       // 今晨起床
  },
  "current_time": "14:00",
  "current_state": "low_energy"    // "high_energy" | "low_energy" | "sleeping"
}
```

### 8.2 接口调用时序

```
用户打开 App
    ↓
全局数据同步（参见 data-sync-status-prd.md）
    ↓ 同步完成后，并行调用：
    ├── /api/high-energy/attribution     → 状态 Tab 第一层 + 第三层 + 第四层
    ├── /api/sleep-window/init           → 状态 Tab 夜间模式 + 今日 Tab 入睡窗口
    ├── /api/today/energy-curve          → 今日 Tab 时间轴
    ├── /api/today/suggestions           → 今日 Tab 行动 pin
    └── Smart Digest LLM 调用            → 状态 Tab 第二层

夜间模式激活后，额外调用：
    └── /api/sleep-readiness             → 状态 Tab 第一层（夜间）+ 第三层（夜间）
```

---

## 九、边界情况与降级

### 9.1 数据缺失场景

| # | 场景 | 状态 Tab 表现 | 今日 Tab 表现 |
|:---:|:---|:---|:---|
| 1 | 新用户，无任何数据 | 核心指标显示"--"，归因不展示，引导连接设备 | 时间轴仅展示时间刻度，无曲线和 pin，引导文案 |
| 2 | 有睡眠数据，无运动数据 | 归因仅展示睡眠相关因子 | 运动类建议 pin 基于通用推荐 |
| 3 | 数据同步失败 | 展示缓存值（参见 `data-sync-status-prd.md`） | 展示上次缓存的曲线和建议 |
| 4 | 归因接口失败 | 第一层正常（高能时长来自同步数据），第三层不展示 | 不影响 |
| 5 | 入睡准备度接口失败 | 夜间模式第一层降级为仅展示推荐入睡时间和明日预估 | 不影响 |
| 6 | 建议接口失败 | 不影响 | 时间轴仅展示曲线和高能区间，无行动 pin |
| 7 | 能量曲线接口失败 | 不影响 | 不展示曲线，仅展示高能时段文字（降级到文字列表） |

### 9.2 时间边界场景

| # | 场景 | 处理方式 |
|:---:|:---|:---|
| 1 | 凌晨 00:00–05:00 打开 | 视为"今晚"，状态 Tab 保持夜间模式，今日 Tab 展示当天数据 |
| 2 | 05:00 后首次打开 | 切换为新一天的白天模式 |
| 3 | 用户在夜间模式切换瞬间打开 | 平滑过渡，不闪烁 |
| 4 | 高能时段跨越预计结束时间 | 以实际数据为准更新，不提前切换夜间模式 |

### 9.3 交互边界场景

| # | 场景 | 处理方式 |
|:---:|:---|:---|
| 1 | 2b Agent 生成的建议时间已过 | 不添加到时间轴，Agent 提示"这个时间已经过了，建议调整" |
| 2 | 今日 Tab 建议 pin 时间冲突 | 视觉上标注时间重叠，不自动合并 |
| 3 | 用户频繁切换 Tab | Tab 内容缓存在内存，切换无加载延迟 |
| 4 | 入睡窗口设置页返回后 | 两个 Tab 数据自动刷新，无需手动下拉 |

---

## 十、首次引导（高能时长概念教育）

### 10.1 触发条件

Onboarding 完成后首次进入首页时触发（仅一次）。

### 10.2 引导内容

采用聚焦浮层（spotlight）形式，分 3 步引导：

| 步骤 | 聚焦元素 | 说明文案 |
|:---|:---|:---|
| 1 | 状态 Tab 核心指标 | "这是你的高能时长——今天你精力充沛、可以高效做事的总时间" |
| 2 | 状态 Tab 高能时段 | "这些是你今天精力最好的时段，重要的事可以安排在这些时间" |
| 3 | 今日 Tab 入口 | "切换到「今日」，查看我们为你准备的精力提升建议" |

### 10.3 引导交互

- 每步点击"下一步"或空白区域进入下一步
- 第 3 步完成后自动切换到今日 Tab，引导完成
- 用户可在任意步骤点击"跳过"关闭引导
- 引导完成/跳过状态本地持久化，不再重复触发
