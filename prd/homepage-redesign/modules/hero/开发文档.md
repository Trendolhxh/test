> Master: ../../主文档.md §L3 / §L8
> Shared: ../../shared/事件数据.md
> External: ../../主文档.md §L8「外部契约」（推荐入睡时间 / 高能时长计算 / 入睡准备度曲线）
> UI Peer: ./界面设计.md
> Module: 核心指标区 · `hero`
> Role: Dev
> Status: Skeleton — 由 prd-dev skill 填入

# 核心指标区 · `hero` · Dev PRD

<!-- TODO: 由 prd-dev skill 按章节分批填入。写前必读上方锚点。 -->
<!-- 特别约束（来自 2026-04-22 需求会）： -->
<!-- - 日/夜双模式 + 手动切换（临时，仅本次有效） -->
<!-- - 状态行 4 时序阶段状态机：T1(in_progress) > T2(just_completed) > E(异常) > T3(曲线式) -->
<!-- - HealthKit 中文运动名映射表由本文档 §2 维护 -->
<!-- - 消费外部契约：推荐入睡时间、高能时长曲线预测点、入睡准备度曲线预测点 -->

## 1. 定位与范围

<!-- TODO: 模块在技术栈的哪一层、边界、外部依赖。 -->

## 2. 数据模型

<!-- TODO:
  - 引用 shared/事件数据.md 的 EventPin 结构（不复制）
  - 新增 Hero 内部数据模型：HeroDisplayState（mode: 'day'|'night', metric_value, bucket, status_line_state, manual_override_until?）
  - HealthKit 运动类型 → 中文 display_name 完整映射表
  - 推荐入睡时间外部契约的消费接口（占位，待 sleep-window 定）
-->

## 3. 状态机

<!-- TODO:
  - 日/夜模式切换状态机（自动 + 手动覆盖 + 手动过期回归自动）
  - 状态行 T1-T2-T3-E 四态转移表（含优先级 T1 > T2 > E > T3）
  - T2 内部时间分段（≤2min / 3-59min / 1-3h / >3h→T3）的定时器行为
-->

## 4. 交互流程（对齐 UI PRD §5）

<!-- TODO: 对 ui-prd §5 每个关键帧写工程视角（前置/动作/成功/失败/耗时预算）。
  重点：
  - 进入 Hero 的首屏数据获取顺序
  - 日/夜自动切换的触发时机（分钟级轮询？事件驱动？）
  - 手动切换的持久化策略（仅内存？sessionStorage？）
-->

## 5. 接口契约

<!-- TODO:
  - 对外暴露：Hero 点击下钻发什么事件（给 energy-detail 消费）
  - 消费：高能时长计算结果、入睡准备度值、事件流最近一条、推荐入睡时间
  - 订阅：HealthKit 数据更新、事件流变更
-->

## 6. 依赖关系

<!-- TODO: 表格列全部依赖、类型、失败时策略。 -->

## 7. 边界条件与异常处理

<!-- TODO: 每个输入通道列 happy + 至少 2 种异常路径。每条异常必须对应 ui-prd 的一个状态；若 UI 没有对应状态 → 停下来报。
  重点：
  - Apple Health 授权撤销
  - 数据空（首次打开 / 无同步）
  - 外部契约「推荐入睡时间」未就绪（sleep-window 尚未产出）的降级策略
-->

## 8. 埋点

<!-- TODO:
  - hero.impression（首次进入视口，属性 mode/bucket/has_event）
  - hero.mode_switch（手动切换，属性 from/to/trigger=manual|auto）
  - hero.status_line_shown（每次状态行变化，属性 phase=T1|T2|T3|E）
  - hero.click（点击主数字下钻，属性 target=energy-detail）
-->

## 9. 性能 / 可观测性

<!-- TODO: 首屏耗时预算、状态行刷新频率上限、关键路径日志、告警阈值。 -->

## 10. 未决工程问题

<!-- TODO: 工程决策层待决项，列 bullet。 -->
