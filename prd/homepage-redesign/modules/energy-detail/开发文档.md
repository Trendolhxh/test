> Master: ../../主文档.md §L3 / §L8
> Shared: ../../shared/事件数据.md
> External: ../../主文档.md §L8「外部契约」（高能时长曲线连续值 / 节律时段判定）
> UI Peer: ./界面设计.md
> Module: 高能时长展开 · `energy-detail`
> Role: Dev
> Status: Skeleton · 2026-04-22 · 由 prd-dev 分批填入

# 高能时长展开 · `energy-detail` · Dev PRD

<!-- TODO: 由 prd-dev skill 按章节分批填入。写前必读上方锚点。 -->
<!-- 工程侧特别约束（映射 ./界面设计.md 2026-04-22 决策）： -->
<!--
  · 外部契约「高能时长曲线」须返回连续预测值（档位边界 v1 不消费；保留字段以便后续版本接入）
  · 节律时段判定：shared/节律时段.md **v1 UI 不消费**；契约保留、供后续版本（节律背景 + 时段药丸回归时）接入。工程侧 v1 不必前置对接此契约
  · 事件模型扩展：shared/事件数据.md 需加 custom_name / dose / intensity / impact.{energy_minutes, sleep_readiness_delta} 字段（v1 数据层存；UI 层对 custom_name 读，其他字段 v2 起消费）
  · 曲线组件化：封装独立组件，为 v3+ 多用户 overlay 的 `dataSource` / `overlay` props 留口；同时为后续"节律背景层"保留一个 `backgroundLayer` 插槽 prop（v1 不启用）
  · 主 metric 抽象：URL/state 带 `metric` 参数（v1 固定 `energy`；v3+ 扩 `illuminance` / `temperature` / `co2` 等）
  · 此刻线：端侧计算，不依赖后端返回
  · 2026-04-22 追加决策：v1 移除节律背景分档色 + 时段药丸；视图简化为「曲线 + 事件 + 此刻线」
-->

## 1. 定位与范围

<!-- TODO: 二级页模块；工程边界；依赖外部契约清单；不承担什么。 -->

## 2. 数据模型

<!-- TODO（v1 版）:
  · EnergyCurvePoint（时序预测值；档位标记字段保留但 v1 UI 不消费）
  · EventPin 扩展字段（custom_name / dose / intensity / impact）
  · DateRange（过去 + 今天；上限待定）
  · ViewState（当前日期 / 滚动位置 / 选中事件 / metric 参数位）
  · RhythmSegment（来自 shared/节律时段.md）—— v1 **不纳入** ViewState；后续节律层回归时再加
-->

## 3. 状态机

<!-- TODO（v1 版）:
  · 加载：请求曲线 + 事件（并行）—— v1 **不**请求节律时段
  · 今日 vs 历史日期的数据来源差异（今日有预测段，历史日期无预测段）
  · 日期切换
  · 事件选中 / 反选
  · 错误与重试
-->

## 4. 交互流程（对齐 UI §5）

<!-- TODO: 对 UI §5 每个关键帧写工程视角（前置 / 动作 / 成功 / 失败 / 耗时预算）。 -->

## 5. 接口契约

<!-- TODO（v1 版）:
  · 消费：高能时长曲线（连续值） · 事件流（6 类 + custom_name）
  · **不消费**：节律时段（契约存在于 shared，v1 UI 不接入）
  · 对外暴露：无（本模块不下钻）
  · 订阅：事件流增删改推送（用户新增 / HealthKit 同步）
-->

## 6. 依赖关系

<!-- TODO: 表格列出依赖 · 类型 · 失败策略 · 是否阻塞渲染。 -->

## 7. 边界条件与异常处理

<!-- TODO（v1 版）:
  · 外部契约「高能时长曲线」未返回连续值（只给分档结论）的降级
  · 历史日期超出存储范围
  · 空数据 / 授权撤销
  · 每异常对应 UI §2 的一个状态；若 UI 无对应 → 停下来报
  · 注：节律时段契约未就绪的降级逻辑 v1 不需要（v1 不消费该契约）；节律层回归时补
-->

## 8. 埋点

<!-- TODO（v1 版）:
  · energy_detail.impression（进入，属性 date / has_events / event_count）
  · energy_detail.scroll（滚动行为，采样）
  · energy_detail.date_switch（切日期，属性 from / to / direction）
  · energy_detail.event_click（点事件，属性 event_type / is_custom_name）
  · energy_detail.back_to_hero（返回）
  · 注：energy_detail.phase_pill_click（点时段药丸）v1 不出现；节律层回归时补
-->

## 9. 性能 / 可观测性

<!-- TODO: 首屏耗时预算 · 曲线渲染采样率 · 滚动帧率 · 关键路径日志 · 告警阈值。 -->

## 10. 未决工程问题

<!-- TODO:
  · 曲线连续值的采样粒度（每分钟 / 每 5 分钟 / 每 15 分钟）
  · 事件流订阅是否走 WebSocket / 轮询
  · 历史数据的本地缓存策略
  · custom_name 在 v1 的存储路径（即使 UI 不显示，也要存）
-->
