# Round 5 · 自洽审查 · 精力详情页
> 审查日期：2026-05-10 | 审查角色：Product Reviewer（新会话） | PRD 版本：v0.1 → v0.3

---

## 1. 互相矛盾

- [驳回] **[P0] AC-01 精力评价阈值边界值模糊**
  **PM 决定**：4 档已明确，边界由服务端实现决定，不在 PRD 细化。

- [接受] **[P1] AC-03 静息心率阈值 vs ADR-01 精度不一致**
  **已修复**：ADR-01 改为连续区间 `<基线 完美 / [基线, 基线+3) 良好 / [基线+3, 基线+10) 需注意 / ≥基线+10 严重影响`，补"新用户无基线→数据积累中"。AC-03 同步更新。

- [接受] **[P1] AC-01 前端评价逻辑 vs API 返回值职责不清**
  **已修复**：AC-01 改为"直接使用 API 返回的 energy_rating，前端不自行计算"。

- [改] **[P2] API sleep.score 范围**
  **已修复**（v0.2）：sleep.score 改为 [0,100]。精力总分公式已在服务端实现，PRD 不重复。

## 2. 未覆盖的 metric

- [驳回] **[P0] 全文零 metric**
  **PM 决定**：基础功能页面，不设独立 metric。

## 3. 未衡量的 AC

- [驳回] **[P1] 全部 AC 均无 metric 关联**
  **PM 决定**：随 §2 一并跳过。

## 4. 决策冲突

- [接受] **[P1] DEC-05 单色柱 vs DEC-01 四档评价的信息断层**
  **已修复**：ADR-05 补充"刻意选择说明"，记录单色是为与现有详情页一致，后续可按数据重新评估。

- [接受] **[P2] DEC-03 睡眠债跳转 vs DEC-04 无数据兜底的交叉盲区**
  **已修复**：AC-04 补"当子维度值为 null 时，该行显示「—」，不可点击，不显示 >"。

## 5. UI vs AC 缺口

- [接受] **[P0] 无 Loading 状态 AC**
  **已修复**（v0.2）：补 AC-13。

- [接受] **[P0] AC-12 缓存态无视觉指示**
  **已修复**：AC-12 补"顶部显示「数据可能非最新」提示条"。

- [接受] **[P1] AC-11 实时刷新机制未定义**
  **已修复**：AC-11 改为"前台刷新"，明确"复用现有 HealthKit observer，切回 App 时触发"。api.yaml 补 `refresh_mechanism` 段。

- [接受] **[P1] 无 Pull-to-Refresh AC**
  **已修复**：补 AC-14（下拉刷新→调 /detail + /trend）。

- [改] **[P2] 月视图 31 柱拥挤但无 AC 约束**
  **已修复**（v0.2）：AC-05 补 slot-based 方案说明。

## 6. 错误/降级完备性

- [接受] **[P1] /detail 和 /trend 部分失败未定义**
  **已修复**：补 AC-15（trend 成功+detail 失败 / 反之 / 全失败三路降级）。

- [接受] **[P1] 缓存策略未定义**
  **已修复**：api.yaml 补 `cache_policy` 段（按日期缓存、TTL 24h、复用现有缓存层）。

- [接受] **[P2] 错误码缺 auth 和通用网络错误**
  **已修复**（v0.2）：api.yaml 补 UNAUTHORIZED / SERVER_ERROR / TIMEOUT。

## 7. 风险最高的 3 条 AC

| AC ID | 风险 | 缓解 | 状态 |
|---|---|---|---|
| AC-03 | 阈值复杂 + 静息心率依赖基线 | ADR-01 改连续区间 + 补"无基线"兜底 | ✅已缓解 |
| AC-11 | iOS HealthKit 后台更新延迟 | 改"前台刷新"措辞，不承诺实时 | ✅已缓解 |
| AC-07 | 缺失组合爆炸 + 测试覆盖难 | 待开发阶段补完整 mock 矩阵 | ⚠️留意 |

---

**Round 5 结论**：16 条中 **13 条接受并修复**、**3 条驳回**（PM 确认）。清单已清空。PRD 版本 bump 至 v0.3。

**Advance Check**：
- [x] 每条问题已标记 接受/驳回/改
- [x] 接受项已回 Round 2/3/4 修复
- [x] 版本已 bump（v0.1 → v0.3）
- [ ] PM 签字（待确认）
