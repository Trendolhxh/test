# 改善 Tab · 工作流进度

**最后更新**：2026-04-23
**最后 commit**：即将 commit（`主文档.md` v1.0 骨架 · L1-L2 完稿 · L3-L8 占位）

---

## 现在在哪

**阶段 1 · 进行中：Master PRD 起草**

- **L1 问题定位** · ✅ 完稿
- **L2 核心对象「改善路径」** · ✅ 完稿
- **L3 首屏结构与气质** · ⏸ 骨架占位（7 个子节名 + 首屏 5 条硬约束名）
- **L4-L8** · ⏸ 骨架占位

**下一步**（按优先级）：

1. 展开 L3.1-L3.7 全部子节 + 5 条硬约束 Do/Don't + 取舍逻辑链路（分 2 次 Edit 写入）
2. L4 Do/Don't 总表 · L5 消失测试（对 `静态诊断文本 vs Agent` / `次位卡 vs 查看更多` / `明白了按钮 vs 直接关闭` 三组候选选边）
3. L6 场景映射（提神 / 助眠入睡两个场景下 §1-§4 的具体示例行）· L7 砍项清单 · L8 模块切分定稿
4. 启动阶段 2（`modules/` 与 `shared/` 骨架建立）

## 四阶段总览

- [ ] 阶段 1 · Master PRD（`主文档.md` 当前 v1.0 · L1-L2 完稿 · L3-L8 待填）
- [ ] 阶段 2 · 模块拆解（待启动）
- [ ] 阶段 3 · 逐模块撰写
- [ ] 阶段 4 · 全局审查

## 已讨论成熟 · 待落盘内容速查

L3 展开将写入的材料，全部已在 2026-04-23 与 PM 的 7 轮产品讨论中确认：

- **L3.1** 首屏结构 v3：§1 目标（提神 / 助眠入睡）/ §2 当前诊断（多维度并列）/ §3 解法树（主位大卡 + 次位小卡 · 1:1 映射诊断）/ §4 延展
- **L3.2** 交互：目标切换整屏刷新（sheet 选择 → 300ms fade-out → loading 一句话 → 400ms fade-in · 总计约 1.2-1.4s）· 禁止部分刷新
- **L3.3** 气质：定位句「读过睡眠医学、愿意晚上 11 点接你电话的朋友」+ 对标反例表（不是 Calm 不是 Oura）+ 11 条 Do/Don't + 视觉基调（低光纸面 · 随时段渐变同 Homepage Hero 气场）+ 声音基调（真人录音 · 温厚中性 · 无默认 bgm）
- **L3.4** 完成态：知识性内容（`明白了` / `没明白·问一下` → Agent 对话）+ 音频类内容（`收藏` / `再听一次` / `换一段`）
- **L3.5** 动态更新：3 源组装（当前场景 / 历史消费 / 偏好库）· 降权规则（短期 7 天不重推同机制 · 收藏升权但非必现 · 显式"换一段"拒绝长期降权该条）
- **L3.6** 查看更多：每张解法卡右上角 `更多 ↗` → 主题深度页（二级）· 不做全局"浏览所有内容"入口
- **L3.7** Agent 登场点：仅在节点消费后 `没明白` / `告诉我感觉` 触发，承接已消费内容 + 画像 + 当前目标作为上下文（窄问题策略 · 以上下文丰富度弥补 Agent 算法不稳定）
- **5 条硬约束**：逻辑可见 / 动态组织 / 主位 CTA 唯一 / 临床源头明示 / 诊断 1:1 映射解法

## 模块矩阵（骨架预告 · L8 待定稿后落盘）

| 模块 | 界面设计 | 开发文档 | 产品笔记 | 原型 |
|---|---|---|---|---|
| `hero` · §1 目标 + §2 当前 + §3 解法树 | ⏸ | ⏸ | — | — |
| `path-extensions` · §4 延展位 | ⏸ | ⏸ | — | — |
| `content-consumption` · 节点消费 + 两套完成态 | ⏸ | ⏸ | — | — |
| `topic-detail` · 主题深度页（二级下钻） | ⏸ | ⏸ | — | — |
| `agent-followup` · 没明白追问对话 | ⏸ | ⏸ | — | — |
| `path-engine` · 底层 · 3 源组装 | — | ⏸ | — | — |
| `profile-engine` · 底层 · 用户画像持续更新 | — | ⏸ | — | — |

**图例**：✅ 完成 / 🟡 进行中 / ⏸ 未开始 / — 本模块不需要

## 外部契约（不在本需求内）

| 契约 | 依赖方 | 状态 |
|---|---|---|
| `推荐入睡时间`（来自 `prd/sleep-window/`）| §1 日 / 夜目标默认值切换（复用 Homepage 的切换机制 `now ≥ 推荐入睡时间 - 3h`）| ⏸ sleep-window 目录已建、master PRD 未落 |
| Homepage 对外暴露的状态摘要字段（分档 × 时段 × 事件流）| §2 当前诊断的输入 | ⏸ 需 Homepage 补契约 |
| Onboarding 画像源（静态） | `profile-engine` 起点 | ⏸ 属独立 PRD，未立项 |

## 待议跨模块 / 跨 Tab 问题

- **节点完成后首屏变化**（A/B/C）——用户倾向 B（主位卡变已完成 · 次位升主位 · 不整屏刷新，区别于"目标切换整屏刷新"），但 PM 未明确选边；待 L5 消失测试阶段一并裁定
- **音频"生成"的边界**——用户原话含"推荐或生成"，但生成式音频与气质约束"真人录音"冲突。初步定义"生成 = AI 挑选组合现有精品内容的个性化 session，不生成全新音频"；待 L4 Do/Don't 阶段写入
- **"这条路径不对"的 v2 升级**——v1 是简单 tag selector，v2 可升级为 Agent 对话；不在本版本交付范围

## 新 session 恢复入口

1. 读本文件 → 定位当前阶段与下一步
2. 读 `主文档.md`（全篇）→ 定位 L1-L2 已定结论 + L3-L8 骨架
3. 若进入 L3 撰写：展开上述「已讨论成熟 · 待落盘内容速查」写入对应子节
4. 若准备进阶段 2：按 L8 模块切分拆 `modules/` 目录

---

## 变更日志

- **2026-04-23** · 改善 Tab 需求立项。与 PM 完成 7 轮产品讨论并收敛：
  - Q1-Q3 场景 / 耦合 / 音频角色：A 场景主（即时不适）· 与 Homepage 强耦合 · 音频是载体之一不是主角
  - 引擎三问 E1-E3：画像多源持续更新 · 中消费为主重消费为辅（轻消费归 Homepage）· 处方不是单点而是完整逻辑链
  - "消费级 CBTI" 类比穿戴设备把医疗仪器消费电子化 / 方法体系多元（不是所有用户都适用 CBTI）
  - 目标导向文案（第一版只做 "提神" "助眠入睡"，后续可扩展目标集合）
  - 树状结构（临床逻辑本就分支 · 产品必须体现 · 诊断 1:1 映射解法）
  - 内容消费完成态按内容类型分叉（知识单次 · 音频多次重复）
  - 3 源动态更新（场景 / 历史 / 偏好）· 查看更多入口（解法卡右上角）· Agent 登场点仅在节点消费后承接已有上下文
- **2026-04-23** · 建 `prd/improve-tab/` 目录 + `主文档.md` v1.0 骨架（~160 行）· L1-L2 完稿 · L3 子节名占位 · L4-L8 骨架占位
