# Function-Calling（工具）说明

> 面向产品 / 运营：了解服务端对话引擎**是否**、**如何**让大模型调用结构化工具，以及各工具**对外能力**的口径。  
> 实现真源：`supabase/functions/_shared/ishoAI/tools/`（`health.ts` / `event.ts` / `cards.ts`），经 `turn-flow` 绑定到模型。

---

## 1. 是否支持 Function-Calling？

**支持。** 在 App 内「聊天」主链路（Unified Turn / `turn-flow`）中，大模型可在多轮对话里发起 **Function-Call**（业界也常称「工具调用」）。服务端执行对应函数，将**结构化结果**再喂回模型，用于：

- 拉取用户**健康数据**（睡眠、心率、步数等）；
- **记录**行为事件（饮食、小睡、吸烟、运动等）并查询历史；
- **下发卡片**（图表、行动建议、调整入睡时间等），由客户端按协议渲染。

**重要**：本回合**实际**开放给模型的工具列表，由「对话技能」里的 **`toolAllowlist` 并集**过滤（例如安全模式、纯危机支持时可能**不绑定**健康工具）。并非每次对话都会带上下面全部函数。

---

## 2. 工具一览（按业务域）

下列 **`name`** 为模型可见的函数名（与协议/埋点一致）。**描述**摘自实现里给模型看的 `description`，已略作排版，便于产品阅读。

### 2.1 健康数据（`health.ts`）

| 函数名 | 产品描述（模型侧说明摘要） |
|--------|----------------------------|
| `get_sleep_data` | 查询最近 N 天「每晚一条」睡眠汇总（深睡/REM/浅睡/清醒分钟、总时长、效率、入睡潜伏期、卧床起床时间等）。可用于趋势；整晚睡眠分期条带需走图表卡的 `sleep_stage_timeline`，勿用本结果冒充 hypnogram。 |
| `get_hrv_data` | 最近 N 天按夜聚合的 HRV（毫秒），含均值/最小/最大/样本数。 |
| `get_heart_rate` | 最近 N 天**日级静息心率**（次/分）。与「睡眠当夜按时间桶的心率」不同。 |
| `get_heart_rate_night` | 最近若干自然日**睡眠期间**按时间桶的心率（min/max/avg）。用于夜间曲线；规范出图指标为 `sleep_heart_rate_night`，一般由健康图表卡 + 客户端二次拉数。 |
| `get_blood_oxygen` | 最近 N 天按夜聚合血氧 SpO₂（%）。 |
| `get_activity` | 最近 N 天每日步数、活动消耗（千卡）等。 |
| `get_workouts` | 最近 N 天锻炼记录（类型、时长、能量、距离等）。 |
| `get_user_profile` | 用户基本信息（如资料字段：性别、生日、推算年龄等），JSON 对象。 |
| `get_daily_health_metrics` | 最近 N 天每日综合指标（压力、睡眠效率、**睡眠一致性**字段、睡眠时长结构等）。注意：**睡眠效率**与**睡眠一致性**含义不同；「一致性」趋势用专用睡眠一致性卡片，勿与效率混淆。 |
| `analyze_food_sleep_impact` | 根据用户描述的食物/饮料，分析咖啡因、酒精、糖、辛辣等成分及对睡眠的影响（服务端会再调小模型生成结构化分析）。需传入食物描述；可选摄入时间、计划入睡时间。 |

**说明**：`get_sleep_segments`（原始睡眠分期片段）当前在代码中**临时关闭**，避免上下文过大；分期展示依赖图表能力与 `sleep_stage_timeline` 等路径。

**登录要求**：健康类 RPC 使用当前登录用户 ID；未登录用户不会绑定健康工具（与实现一致）。

---

### 2.2 事件记录与查询（`event.ts`）

| 函数名 | 产品描述（模型侧说明摘要） |
|--------|----------------------------|
| `record_event` | 记录睡眠相关**行为事件**，并检查必填槽位是否齐全。事件类型：`sleep`（含小睡/nap）、`diet`、`smoking`、`exercise`。返回 `recorded`（已落库）或 `partial`（缺槽位，模型可追问）；饮食类在信息足够时服务端会减少无意义追问（如默认单位、时间）。 |
| `get_user_events` | 查询用户近期行为事件；可按 `event_type`（如 diet / smoking / exercise / nap）及饮食子类型 `event_subtype`（caffeine / alcohol / snack）过滤。 |

---

### 2.3 卡片建议（`cards.ts`）

模型**不直接画 UI**，而是调用下列函数，由服务端生成 **`structured.cards`**，客户端按 `cardType` 渲染。

| 函数名 | 产品描述（模型侧说明摘要） |
|--------|----------------------------|
| `suggest_action_card` | 主决策行动卡：标题 + 若干可点击动作（受策略约束数量）。适合需要用户明确「下一步点啥」时。 |
| `suggest_high_energy_window_card` | 今日**高精力时间窗口**卡，含起止时间与可选依据，客户端可画区间。 |
| `suggest_sleep_time_adjust_card` | 用户已过建议入睡时间仍未睡时，下发「调整入睡时间」滑块类卡片，客户端回传调整结果。 |
| `suggest_sleep_consistency_card` | **睡眠一致性 / 作息规律**趋势（不是「睡眠效率%」）。独立卡类型 `sleepConsistencyChartCard`；勿与健康大图卡的 metricKey 混用。 |
| `suggest_health_chart_card` | 标准健康大图：必须带 **metricKey + timeRange**，由客户端按 key 二次拉数。**规范指标 key**包括：睡眠分期条带、静息心率、睡眠当夜心率、步数、活动消耗、睡眠效率、血氧、HRV 等（实现内有固定枚举，模型不可随意编造 key）。 |
| `suggest_inline_chart_card` | **内联小图**：2～24 个数据点直出，适合已从健康工具拿到 `rows` 后的轻量趋势。**不能**用来冒充上述规范大图指标，也不能用来画「睡眠一致性」专用趋势（须用睡眠一致性卡）。 |

**备注**：`suggest_feedback_card` 在代码中当前为注释状态，未注册进默认工具列表。


